/.idea

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Артефакты запусков EagleTrade
project/run_out.txt
project/results/
project/stvgp/results/
# Временные файлы инкрементального сохранения (filelock)
*.lock
*.tmp
# evolution_results.json НЕ игнорируем — это результат эволюции,
# версионируем его, чтобы переносить между машинами через git pull/push.

# Сгенерированные графики (создаются кодом при каждом прогоне)
backtest_result.png
equity_bull_feb2024.png
equity_bear_may2023.png
summary_bars.png
