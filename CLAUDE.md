# EagleTrade

## Деплой на сервер (Proxmox)

Проект на сервере лежит в `/opt/mybot/app`, виртуальное окружение — `/opt/mybot/bin`.

Запуск на сервере:
```bash
source /opt/mybot/bin/activate
cd /opt/mybot/app
```
